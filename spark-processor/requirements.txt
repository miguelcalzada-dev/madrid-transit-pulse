"""
============================================================
requirements.txt - Dependencias del procesador Spark
Madrid Transit Pulse: Spark Processor
============================================================
Instalar con: pip install -r requirements.txt

Nota: pyspark debe coincidir con la versión de Spark instalada.
Para desarrollo local, usar Spark 3.5.x con Python 3.10+
"""

# Apache Spark con soporte para Structured Streaming y Kafka
pyspark==3.5.0

# MongoDB connector para Spark (sink final del pipeline)
# Artefacto Maven: org.mongodb.spark:mongo-spark-connector_2.12:10.3.0
# Se pasa como --packages al submit de Spark

# Kafka connector para Spark Structured Streaming
# Artefacto Maven: org.apache.spark:spark-sql-kafka-0-10_2.12:3.5.0
# Se pasa como --packages al submit de Spark

# Utilidades para desarrollo y testing
python-dotenv==1.0.0      # Cargar variables de entorno desde .env
loguru==0.7.2             # Logging mejorado con colores y formato
pytest==7.4.3             # Framework de testing
pytest-mock==3.12.0       # Mocking para tests unitarios
